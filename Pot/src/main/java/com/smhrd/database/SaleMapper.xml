<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="SaleMapper">

	<insert id="write" parameterType="com.smhrd.model.PotSale">
		insert into tb_sale(sale_category, sale_title, user_id, sale_price, sale_img, sale_content) values (#{sale_category}, #{sale_title}, #{user_id}, #{sale_price}, #{sale_img}, #{sale_content})
	</insert>
	
	<!-- 여러개의 행을 가져올 때: resultType -> 리스트 내의 각 아이템의 타입 -->
	<select id="select" resultType="com.smhrd.model.PotSale">
		select * from tb_sale order by sale_idx desc
	</select>
	
	
	<select id="getBoard" parameterType="int" resultType="com.smhrd.model.PotSale">
		select * from tb_sale where sale_idx=#{sale_idx}
	</select>
</mapper>