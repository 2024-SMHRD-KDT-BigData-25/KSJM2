<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- namespace: mapper의 식별자 -->
<mapper namespace="PotMapper">

	<insert id="insert">
		insert into users(user_id, user_pw, user_name, user_nick, user_phone, user_post, user_addr1, user_addr2)
		values(#{user_id}, #{user_pw}, #{user_name}, #{user_nick}, #{user_phone}, #{user_post}, #{user_addr1}, #{user_addr2})
	</insert>
	
	
	<select id="idcheck" resultType="int">
		select count(id) from users where id=#{id}
	</select>
	
</mapper>


